name: HeadConverter
version: '${project.version}'
main: dev.Fjc.headConverter.HeadConverter
api-version: '1.21'
prefix: HeadConverter
load: STARTUP
authors: [ Fjc ]
description: With pets being gone, this plugin will convert old pets into intangible currency.

commands:
  petexchanger:
    description: "Allows you exchange pets for currency."
